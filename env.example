# ============================================================================
# Find Your Word Widget - Environment Configuration
# ============================================================================
#
# IMPORTANT: This file shows example values. Copy this file to .env and
# add your actual values. Never commit the .env file to version control!
#
# For production deployment, configure these on your Digital Ocean server.
# ============================================================================

# ----------------------------------------------------------------------------
# SERVER CONFIGURATION (Required for server.js)
# These are used by the Node.js server to proxy requests to OpenAI
# ----------------------------------------------------------------------------

# OpenAI API Key (Server-side only - NEVER exposed to client)
# Get this from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# OpenAI Prompt ID for stored prompt
# This is the ID of your OpenAI stored prompt
OPENAI_PROMPT_ID=pmpt_your_prompt_id_here

# Server Port (optional, defaults to 8080)
PORT=8080

# ----------------------------------------------------------------------------
# WIDGET BUILD CONFIGURATION (Required for npm run build)
# These are baked into the widget bundle at build time
# ----------------------------------------------------------------------------

# API Base URL - Your Digital Ocean server URL
# Development: http://localhost:8080
# Production: https://your-server.ondigitalocean.app
VITE_API_BASE_URL=http://localhost:8080

# Debug Mode (optional, defaults to false)
# Set to 'true' for verbose logging during development
VITE_DEBUG_MODE=false

# ============================================================================
# DEPLOYMENT NOTES
# ============================================================================
#
# LOCAL DEVELOPMENT:
# 1. Copy this file to .env
# 2. Add your OPENAI_API_KEY and OPENAI_PROMPT_ID
# 3. Use VITE_API_BASE_URL=http://localhost:8080
# 4. Run: npm install && npm run dev (for widget dev)
# 5. Run: npm start (in separate terminal, for server)
#
# PRODUCTION (Digital Ocean):
# 1. Set environment variables in Digital Ocean App Platform:
#    - OPENAI_API_KEY (from OpenAI dashboard)
#    - OPENAI_PROMPT_ID (your stored prompt ID)
#    - PORT (usually set automatically by DO)
#
# 2. Build widget with production API URL:
#    - Set VITE_API_BASE_URL=https://your-server.ondigitalocean.app
#    - Run: npm run build
#    - Upload dist/ folder to server
#
# 3. Deploy server:
#    - Push to Digital Ocean (server.js will start automatically)
#    - Server will use environment variables set in DO dashboard
#
# SECURITY:
# - API key is ONLY on the server (never in widget bundle)
# - Widget calls YOUR server, server calls OpenAI
# - Shopify loads widget, widget calls your API
#
# ============================================================================

